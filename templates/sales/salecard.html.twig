{% extends 'base.html.twig' %}
{% block body %}
    <a href="{{ path('sales') }}" class="m-3">Назад</a>
    <div class="container">
        <div class="row mt-5">
            {{ form_start(form) }}

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}