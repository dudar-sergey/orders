{% extends 'base.html.twig' %}
{% block body %}
    <div class="content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <a href="{{ path('syncFromEbay') }}">
                        <button class="btn btn-primary mb-3"><i class="fas fa-sync"></i> Синхронизировать из eBay</button>
                    </a>
                    <a href="{{ path('createKit') }}">
                        <button class="btn btn-primary mb-3"><i class="fas fa-cogs"></i> Создать комплект</button>
                    </a>
                    <div class="card striped-with-hover">
                        <div class="card-body table-full-width table-responsive">
                            <div class="card-body table-full-width table-responsive">
                                <table class="tsort table table-hover table-striped" id="mytable">
                                    <thead>
                                    <th scope="col">Изображение</th>
                                    <th scope="col">ID товара</th>
                                    <th scope="col">Ариткул</th>
                                    <th scope="col">UPC</th>
                                    <th scope="col">Название</th>
                                    <th scope="col">Стоимость</th>
                                    <th scope="col">Количество</th>
                                    <th>Карточка</th>
                                    </thead>
                                    <tbody>
                                    {% for product in products %}
                                        <tr>
                                            <td><img src="{{ product.images.first.url ?? 'https://cdn.pixabay.com/photo/2015/12/09/22/38/camera-1085705_960_720.png' }}" width="100" height="100" alt=""></td>
                                            <td title="">{{ product.allegroOffer.allegroId ?? product.eId }}</td>
                                            <td>{{ product.articul }}</td>
                                            <td>{{ product.upc }}</td>
                                            <td><a href="{{ product.url }}">{{ product.name }}</a></td>
                                            <td>
                                                {{ product.price}}
                                            </td>
                                            <td>{{ product.quantity }}</td>
                                            <td><a href="{{ path('cardItem', {'itemId': product.id}) }}">Карточка</a></td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}