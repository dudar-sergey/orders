<!DOCTYPE html>
<html lang="ru">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}АБВГД{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">
            <meta content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0, shrink-to-fit=no' name='viewport'>
            <link href="https://fonts.googleapis.com/css?family=Montserrat:400,700,200" rel="stylesheet">
            <link rel="stylesheet" href="{{ asset('/assets/css/light-bootstrap-dashboard.css') }}">
            <link rel="stylesheet" href="{{ asset('/assets/style.css') }}">
        {% endblock %}
    </head>
    <body>
    {% if app.user %}
    {% endif %}
    <div class="wrapper">
        <div class="sidebar shadow-lg" data-image="../assets/img/sidebar-5.jpg">
            <div class="sidebar-wrapper">
                <div class="logo">
                    <a href="{{ path('home') }}" class="simple-text">
                        Система
                    </a>
                </div>
                <ul class="nav">
                    <li>
                        <a class="nav-link" href="{{ path('userprofile') }}">
                            <i class="far fa-user-circle"></i>
                            <p>Профиль</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('dynamics') }}">
                            <i class="fas fa-chart-line"></i>
                            <p>Динамика</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('table') }}">
                            <i class="fas fa-table"></i>
                            <p>Табло</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('orders') }}">
                            <i class="fas fa-clipboard-list"></i>
                            <p>Заказы</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('products') }}">
                            <i class="fas fa-box"></i>
                            <p>Склад</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('supplies') }}">
                            <i class="fas fa-truck-loading"></i>
                            <p>Поставки</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('sales') }}">
                            <i class="fas fa-dollar-sign"></i>
                            <p>Продажи</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('handbook') }}">
                            <i class="fas fa-file-alt"></i>
                            <p>Справочник</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('unload') }}">
                            <i class="fas fa-cloud-upload-alt"></i>
                            <p>Выгрузка</p>
                        </a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ path('update') }}">
                            <i class="fas fa-retweet"></i>
                            <p>Обновления</p>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
        <div class="main-panel">
            <nav class="navbar navbar-expand-lg">
                <div class="container-fluid">
                    <div class="collapse navbar-collapse justify-content-end" id="navigation">
                        <ul class="navbar-nav ml-auto">
                            {% if app.user %}
                            {% if app.session.get('currentProfile') %}
                                <div class="nav-item">
                                    <div class="nav-link">
                                        <i class="text-muted">Выбранный профиль <b>{{ app.session.get('currentProfile').name }} {{ app.session.get('currentProfile').userName }}</b></i>
                                    </div>
                                </div>
                                {% endif %}
                                <div class="nav-item">
                                    <div class="nav-link">
                                        <i class="text-muted">Пользователь {{ app.user.username }}</i>
                                    </div>
                                </div>
                            {% endif %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('userprofile') }}">
                                    <span class="no-icon">Профиль</span>
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_logout') }}">
                                    <span class="no-icon">Выйти</span>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        {% block body %}
        {% endblock %}
        </div>
    </div>
    <div class="notification shadow" id="notification">
        <div class="toast-body message" id="message">
        </div>
    </div>
        {% block javascripts %}
            <script src="//cdn.jsdelivr.net/npm/sweetalert2@10"></script>
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
            <script src="{{ asset('assets/chosen/chosen.jquery.min.js') }}"></script>
            <script src="https://kit.fontawesome.com/8289a35b8d.js" crossorigin="anonymous"></script>
            <script src="{{ asset('assets/tsort.min.js') }}"></script>
            <script src="{{ asset('assets/myJs/main.js') }}"></script>
            <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
        {% endblock %}
    </body>
</html>
